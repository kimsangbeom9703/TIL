# [JAVA - 점프 투 스프링부트 #03] 

# 📁 스프링 부트 프로젝트 구조

```
jump2springboot
├── .gradle/
├── .idea/
├── build/
├── gradle/
│   └── wrapper/
│       ├── gradle-wrapper.jar
│       └── gradle-wrapper.properties
├── src/
│   ├── main/
│   │   ├── java/
│   │   │   └── ksb.jump2springboot/
│   │   │       ├── HelloController.java
│   │   │       └── Jump2springbootApplication.java
│   │   └── resources/
|   |   |   └── static
|   |   |   └── templates
|   |   |   └── application.properties
│   └── test/
│       └── java/
│           └── ksb.jump2springboot/
│               └── Jump2springbootApplicationTests.java
├── .gitattributes
├── .gitignore
├── build.gradle
├── gradlew
├── gradlew.bat
├── HELP.md
└── settings.gradle
```

## src/main/java
```
src/main/java 디렉터리는 자바 파일을 저장하는 공간이다.
```

## ksb/jump2springboot/
```
이 패키지는 ksb의 자바 파일을 저장하는 공간이다 .
HelloController.java와 같은 스프링 부트의 컨트롤러, 폼과 DTO, 데이터베이스 처리를 위한 엔티티, 서비스 등의 자바 파일이 이 곳에 위치한다.

    컨트롤러는 URL 요청을 처리하고 폼은 사용자의 입력을 검증한다. 
    DTO, 엔티티, 서비스 파일은 데이터베이스를 처리하기 위해 필요한 파일이다
```

## Jump2springbootApplication.java
```
프로그램의 시작을 담당하는 파일
클래스에는 반드시 @SpringBootApplication 애너테이션이 적용되어 있어야 한다. 
@SpringBootApplication 애너테이션을 통해 스프링 부트 애플리케이션을 시작할 수 있다.
```

# src/main/resources
```
src/main/resources 디렉터리는 자바 파일을 제외한 HTML, CSS, 자바스크립트, 환경 파일 등을 저장하는 공간이다.
```

## templates 디렉터리
```
src/main/resources 디렉터리의 하위 디렉터리인 templates에는 템플릿 파일을 저장한다.
템플릿 파일은 자바 코드를 삽입할 수 있는 HTML 형식의 파일로, 스프링 부트에서 생성한 자바 객체를 HTML 형태로 출력할 수 있다.
```
## static 디렉터리
```
static 디렉터리에는 프로젝트의 스타일시트(css 파일), 자바스크립트(js 파일) 그리고 이미지 파일(jpg 파일, png 파일 등) 등을 저장한다.
```
## application.properties
```
프로젝트의 환경 변수, 데이터베이스 등의 설정을 이 파일에 저장한다.
```
## src/test/java 디렉터리
```
src/test/java 디렉터리는 프로젝트에서 작성한 파일을 테스트하는 코드를 저장하는 공간이다. 
JUnit과 스프링 부트의 테스트 도구를 사용하여 서버를 실행하지 않은 상태에서 src/main/java 디렉터리에 작성한 코드를 테스트할 수 있다.

    JUnit은 테스트 코드를 작성하고, 작성한 테스트 코드를 실행할 때 사용하는 자바의 테스트 프레임워크이다.
```
## build.gradle 파일 
```
build.gradle은 그레이들(Gradle)이 사용하는 환경 파일이다. 그레이들은 그루비(Groovy)를 기반으로 한 빌드 도구로 Ant, Maven과 같은 이전 세대의 단점을 보완하고 장점을 취합하여 만들었다. build.gradle 파일에는 프로젝트에 필요한 플러그인과 라이브러리를 설치하기 위한 내용을 작성한다.

    그루비는 그레이들 빌드 스크립트를 작성하는 데 사용하는 스크립트 언어로, 문법이 간결하고 가독성이 높다.
    빌드 도구는 소스 코드를 컴파일하고 필요한 라이브러리를 내려받을 때 사용한다.
    프로젝트를 완성하면 단 한 개의 jar 파일로 패키징하여 서버에 배포할 수 있는데 이때에도 역시 빌드 도구를 사용한다.
```

## 계층형 구조
```
src/
└── main/
    ├── java/
    │   └── com/example/myapp/
    │       ├── MyAppApplication.java     ← 메인 클래스
    │
    │       ├── config/                   ← 설정 클래스 (Swagger, Security 등)
    │       │   └── SwaggerConfig.java
    │
    │       ├── controller/               ← REST API 컨트롤러
    │       │   └── UserController.java
    │
    │       ├── service/                  ← 비즈니스 로직
    │       │   ├── UserService.java
    │       │   └── impl/
    │       │       └── UserServiceImpl.java
    │
    │       ├── repository/               ← DB 접근 (JPA)
    │       │   └── UserRepository.java
    │
    │       ├── entity/                   ← JPA Entity
    │       │   └── User.java
    │
    │       ├── dto/                      ← 요청/응답용 DTO
    │       │   ├── UserRequestDto.java
    │       │   └── UserResponseDto.java
    │
    │       ├── exception/                ← 예외 처리 관련
    │       │   ├── GlobalExceptionHandler.java
    │       │   └── UserNotFoundException.java
    │
    │       └── util/                     ← 유틸 클래스
    │           └── DateUtil.java
    │
    └── resources/
        ├── application.properties (또는 application.yml)
        └── static/      ← 정적 파일 (html, js, css)
        └── templates/   ← 템플릿 파일 (Thymeleaf 등)
```

## 📁 계층형 디렉토리 구조 설명
```
com.example.demo  (← 보통 회사 도메인 + 프로젝트 이름)
├── DemoApplication.java       👉 메인 클래스 (Spring Boot 시작점)
├── config/                    👉 설정 클래스 모음
├── controller/                👉 API 요청을 받는 곳 (웹 진입 지점)
├── dto/                       👉 데이터 전송용 객체 (계층 간 데이터 전달)
├── entity/                    👉 JPA 엔티티 (DB 테이블과 매핑)
├── repository/                👉 DB 접근 인터페이스 (DAO 역할)
└── service/                   👉 비즈니스 로직 담당
    └── impl/                  👉 서비스 구현체 위치 (선택 사항)
```

## 📁 각 폴더 상세 설명
```
폴더 이름 | 설명
DemoApplication.java | Spring Boot 어플리케이션의 시작점. @SpringBootApplication이 붙은 메인 클래스.
config/ | 설정 관련 클래스 (WebConfig, SecurityConfig, SwaggerConfig, 등). 환경 셋팅을 담당.
controller/ | @RestController 클래스들이 위치. 클라이언트 요청을 받아 Service에 전달. URL과 API 매핑 역할.
dto/ | Data Transfer Object. Controller ↔ Service 간 데이터 전달용. 외부에 노출될 데이터 구조만 포함.
entity/ | JPA Entity. DB 테이블과 1:1 매핑되는 클래스들. @Entity, @Id 등의 어노테이션이 사용됨.
repository/ | Spring Data JPA 인터페이스가 위치. JpaRepository, CrudRepository 등을 상속해 DB 쿼리 수행.
service/ | 핵심 비즈니스 로직을 처리하는 인터페이스. 보통 컨트롤러가 이걸 호출해서 기능 처리.
service/impl/ | Service 인터페이스를 실제로 구현하는 클래스들. 실제 로직이 이 안에 들어감. 보통 @Service 붙임.
```

## 💡 용어 간단 요약
```
용어 | 의미
Entity | 데이터베이스 테이블과 매핑되는 클래스
DTO | Entity를 그대로 노출하지 않기 위해 사용하는 데이터 전달 객체
Controller | 클라이언트 요청을 받아 처리하고, 응답을 돌려주는 계층
Service | 컨트롤러에서 요청받은 작업을 처리하는 비즈니스 계층
Repository | 데이터베이스에 접근하기 위한 계층 (DAO의 역할)
```

## 도메인별 구조
```
📂 backend
 ├── 📂 Users
 │    ├── 📂 controller
 │    │    └── UsersController.java  # 회원 관련 요청 처리
 │    ├── 📂 service
 │    │    └── UsersService.java     # 회원 비즈니스 로직
 │    ├── 📂 repository
 │    │    └── UsersRepository.java  # 회원 데이터 접근
 │    ├── 📂 domain
 │    │    └── Users.java            # 회원 데이터 모델
 │    └── 📂 dto
 │         └── UsersDto.java         # 회원 요청/응답 데이터 객체
 ├── 📂 Api
 │    ├── 📂 controller
 │    │    └── ApiController.java  # 검색 관련 요청 처리
 │    ├── 📂 service
 │    │    └── ApiService.java     # 검색 비즈니스 로직
 │    ├── 📂 repository
 │    │    └── ApiRepository.java  # 검색 데이터 접근
 │    ├── 📂 domain
 │    │    └── Api.java            # 검색 데이터 모델
 │    └── 📂 dto
 │         └── ApiDto.java         # 검색 요청/응답 데이터 객체
```

## 초보자용 추천 구조
```
src/main/java/com/example/project
├── controller       // API 요청/응답 처리
├── service          // 비즈니스 로직
├── repository       // DB 접근
├── dto              // 요청/응답 객체
├── entity           // JPA 엔티티
├── exception        // 예외 처리 관련 클래스
│   ├── GlobalExceptionHandler.java
│   ├── CustomException.java
│   ├── ErrorCode.java
│   └── ErrorResponse.java
```